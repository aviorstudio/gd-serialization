name: 'Setup Godot'
description: 'Install Godot binary with caching and optionally install export templates'
inputs:
  godot-version:
    description: 'Godot version to install (e.g., 4.4.1)'
    required: true
  godot-sha256:
    description: 'SHA-256 of the immutable Godot release archive'
    required: true
  install-templates:
    description: 'Whether to install export templates'
    required: false
    default: 'false'

runs:
  using: 'composite'
  steps:
    - name: Cache Godot binary
      id: cache-godot
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
      with:
        path: ~/.cache/godot/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64.zip
        key: godot-${{ inputs.godot-version }}-${{ inputs.godot-sha256 }}-${{ runner.os }}

    - name: Install Godot
      if: steps.cache-godot.outputs.cache-hit != 'true'
      shell: bash
      run: |
        mkdir -p ~/.cache/godot
        curl --fail --location --silent --show-error \
          --output ~/.cache/godot/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64.zip \
          https://github.com/godotengine/godot/releases/download/${{ inputs.godot-version }}-stable/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64.zip

    - name: Verify and install Godot
      shell: bash
      run: |
        set -euo pipefail
        archive="$HOME/.cache/godot/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64.zip"
        printf '%s  %s\n' '${{ inputs.godot-sha256 }}' "$archive" | sha256sum --check
        unzip -oq "$archive" -d "$RUNNER_TEMP/godot"
        chmod +x "$RUNNER_TEMP/godot/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64"
        sudo install -m 0755 "$RUNNER_TEMP/godot/Godot_v${{ inputs.godot-version }}-stable_linux.x86_64" /usr/local/bin/godot

    - name: Verify Godot installation
      shell: bash
      run: |
        test "$(godot --version)" = '${{ inputs.godot-version }}.stable.official.ed1daf0bf'

    - name: Cache Godot export templates
      if: inputs.install-templates == 'true'
      id: cache-godot-templates
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
      with:
        path: ~/.local/share/godot/export_templates/${{ inputs.godot-version }}.stable
        key: godot-templates-${{ inputs.godot-version }}-${{ runner.os }}

    - name: Install Godot export templates
      if: inputs.install-templates == 'true' && steps.cache-godot-templates.outputs.cache-hit != 'true'
      shell: bash
      run: |
        mkdir -p ~/.local/share/godot/export_templates/${{ inputs.godot-version }}.stable
        wget -q https://github.com/godotengine/godot/releases/download/${{ inputs.godot-version }}-stable/Godot_v${{ inputs.godot-version }}-stable_export_templates.tpz
        unzip -q Godot_v${{ inputs.godot-version }}-stable_export_templates.tpz
        mv templates/* ~/.local/share/godot/export_templates/${{ inputs.godot-version }}.stable/

    - name: Verify Godot templates
      if: inputs.install-templates == 'true'
      shell: bash
      run: ls -la ~/.local/share/godot/export_templates/${{ inputs.godot-version }}.stable/
